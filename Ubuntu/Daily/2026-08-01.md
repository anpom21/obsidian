---
created: 2026-08-02
tags:
source:
aliases:
---

- [ ] [[Classification]] make configs more modularized
- [ ] Make sweeps into experiments
- [ ] Modularize augmentations
- [ ] [[Sandcastle]] make a claude issue publisher skill. It should pull from the Projects and task page from notion and the obsidian vault where it should look for any todo with a #sandcastle tag. 
- [ ] [[Sandcastle]] make a sandcastle config that creates a branch instead of just one commit, but make the option between committing and branching easy, so both can be chosen depending on the scope.


- [ ] [[Classification]] add timestamp to the end of model id
- [ ] [[Classification]] make the `generate_manifest.py` script imbed trained config to the .ckpt file. First it should be stripped to just a .pt file leaving only the model structure and then the config should be added.  After adding this implementation then make a model_import.py script. And possibly add it to inference repo.
- [ ] [[Classification]] make the generate manifest script check that no other model has a matching model id. And if there is then prompt the user if it should continue and then bump the version number to the next free minor version eg v0.0.(x+1). 
- [ ] Also from now on the model name should be prefixed the model id